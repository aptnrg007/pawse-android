<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Pawse" parent="android:Theme.Material.Light.NoActionBar" />

    <!--
      BlockActivity: translucent so the blocked app's last frame stays visible behind
      the scrim + avatar, instead of a solid color hiding it outright. Touch input is
      still fully captured by this Activity — nothing behind it is reachable.
    -->
    <style name="Theme.Pawse.Translucent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
