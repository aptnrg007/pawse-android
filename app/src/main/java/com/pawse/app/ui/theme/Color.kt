package com.pawse.app.ui.theme

import androidx.compose.ui.graphics.Color

// Backgrounds
val Background = Color(0xFFF5F2FA)
val Surface = Color(0xFFFFFFFF)

// Hero + accent cards — soft, cycled by index across the app list
val Lavender = Color(0xFFDCCFF2)
val SkyBlue = Color(0xFFC9E9F6)
val BlushPink = Color(0xFFF9D8E0)
val MintGreen = Color(0xFFD3EFDD)

val AccentCards = listOf(SkyBlue, BlushPink, MintGreen, Lavender)

// Text
val TextPrimary = Color(0xFF1B1B1F)
val TextSecondary = Color(0xFF6B6874)

// Buttons — dark pill buttons, not default Material purple
val ButtonDark = Color(0xFF1B1B1F)
val OnButtonDark = Color(0xFFFFFFFF)

// Status
val Danger = Color(0xFFC62828)
val Success = Color(0xFF2E7D32)
